apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-pvc
  namespace: default
spec:
  resources:
    requests:
      storage: 80Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes